export type FlagQuestion = {
  name: string;
  flag: string;
  flagString: string;
};

export type CapitalQuestion = {
  capital: string;
  country: string;
};

export type GameStatus = "playing" | "gameOver";
